---
/**
 * Homepage - Ted's Voice Academy
 * Content source: TVA_Homepage_Copy_Forward_Light_v3.md
 */

import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout 
  title="Ted's Voice Academy"
  description="Voice coaching for singers, speakers, and ensembles. With 40 years of experience, Ted helps you discover your authentic voice through personalized coaching in Lacey, WA or virtually anywhere."
>
  <!-- Hero Section -->
  <section class="hero bg-navy">
    <div class="hero__container container">
      <div class="hero__content">
        <h1 class="hero__title">
          Your Voice Has Something to Say
        </h1>
        <p class="hero__subtitle">
          I help singers, speakers, and ensembles discover what makes their voice uniquely powerful.
        </p>
        <div class="hero__cta">
          <a href="/contact" class="btn btn--primary">Start Your Journey</a>
          <a href="/about" class="btn btn--secondary">Learn More About Ted</a>
        </div>
      </div>
      <div class="hero__image">
        <!-- Placeholder for Ted's hero photo -->
        <div class="hero__image-placeholder">
          <span>Ted's Photo</span>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Intro Section -->
  <section class="intro bg-light">
    <div class="container intro__container">
      <h2 class="intro__title">Voice Coaching That Meets You Where You Are</h2>
      <p class="intro__text">
        Whether you're preparing for a keynote, finding your singing voice, or building ensemble unity, 
        I bring four decades of experience to help you achieve your goals. My approach is simple: 
        we work together to discover what your voice can do, then build the skills to do it consistently.
      </p>
    </div>
  </section>
  
  <!-- Services Section -->
  <section class="services bg-cream">
    <div class="container">
      <h2 class="services__title text-center">How I Can Help</h2>
      <div class="services__grid">
        <!-- Singing Card -->
        <article class="service-card">
          <div class="service-card__icon">
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M9 18V5l12-2v13"/>
              <circle cx="6" cy="18" r="3"/>
              <circle cx="18" cy="16" r="3"/>
            </svg>
          </div>
          <h3 class="service-card__title">Singing</h3>
          <p class="service-card__text">
            Build a voice that's free, powerful, and authentically yours. From beginners to professionals, 
            we'll work on technique that serves your unique artistic expression.
          </p>
          <a href="/singing" class="service-card__link">
            Explore Singing Coaching
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="5" y1="12" x2="19" y2="12"/>
              <polyline points="12 5 19 12 12 19"/>
            </svg>
          </a>
        </article>
        
        <!-- Speaking Card -->
        <article class="service-card">
          <div class="service-card__icon">
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
            </svg>
          </div>
          <h3 class="service-card__title">Speaking</h3>
          <p class="service-card__text">
            Communicate with confidence and impact. Whether it's presentations, meetings, or everyday 
            conversations, develop a speaking voice that commands attention.
          </p>
          <a href="/speaking" class="service-card__link">
            Explore Speaking Coaching
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="5" y1="12" x2="19" y2="12"/>
              <polyline points="12 5 19 12 12 19"/>
            </svg>
          </a>
        </article>
        
        <!-- Ensembles Card -->
        <article class="service-card">
          <div class="service-card__icon">
            <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
              <circle cx="9" cy="7" r="4"/>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
            </svg>
          </div>
          <h3 class="service-card__title">Ensembles</h3>
          <p class="service-card__text">
            Create a unified sound that's greater than the sum of its parts. I work with choirs, 
            vocal groups, and worship teams to build blend, balance, and musical connection.
          </p>
          <a href="/ensembles" class="service-card__link">
            Explore Ensemble Coaching
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="5" y1="12" x2="19" y2="12"/>
              <polyline points="12 5 19 12 12 19"/>
            </svg>
          </a>
        </article>
      </div>
    </div>
  </section>
  
  <!-- Authority Section (AVF & PASS) -->
  <section class="authority bg-navy">
    <div class="container">
      <h2 class="authority__title text-center">Beyond Coaching</h2>
      <p class="authority__subtitle text-center">
        Four decades of teaching have taught me that great voice work goes beyond lessons. 
        These tools help you understand and develop your voice on a deeper level.
      </p>
      
      <div class="authority__grid">
        <!-- AVF Card -->
        <article class="authority-card authority-card--avf">
          <div class="authority-card__content">
            <h3 class="authority-card__title">The Adaptive Voice Framework</h3>
            <p class="authority-card__text">
              My book synthesizes everything I've learned about voice into a practical framework. 
              It's an owner's manual for your voice—useful for singers, speakers, teachers, and anyone 
              who relies on their voice.
            </p>
            <a href="/avf" class="btn btn--avf">Learn About AVF</a>
          </div>
          <div class="authority-card__image">
            <!-- Book cover placeholder -->
            <div class="authority-card__image-placeholder">AVF Book</div>
          </div>
        </article>
        
        <!-- PASS Card -->
        <article class="authority-card authority-card--pass">
          <div class="authority-card__content">
            <h3 class="authority-card__title">PASS Profile</h3>
            <p class="authority-card__text">
              Discover your communication style with this assessment tool I developed. 
              Understand how you naturally communicate and how to adapt when needed.
            </p>
            <a href="/pass-profile" class="btn btn--pass">Explore PASS Profile</a>
          </div>
          <div class="authority-card__image">
            <!-- PASS logo placeholder -->
            <div class="authority-card__image-placeholder">PASS Logo</div>
          </div>
        </article>
      </div>
    </div>
  </section>
  
  <!-- Testimonials Section -->
  <section class="testimonials bg-light">
    <div class="container">
      <h2 class="testimonials__title text-center">What Students Say</h2>
      <div class="testimonials__grid">
        <!-- Placeholder testimonials -->
        <blockquote class="testimonial">
          <p class="testimonial__text">
            "Testimonial placeholder — real student quotes will be added here."
          </p>
          <footer class="testimonial__footer">
            <cite class="testimonial__author">— Student Name</cite>
            <span class="testimonial__role">Singing Student</span>
          </footer>
        </blockquote>
        
        <blockquote class="testimonial">
          <p class="testimonial__text">
            "Testimonial placeholder — real student quotes will be added here."
          </p>
          <footer class="testimonial__footer">
            <cite class="testimonial__author">— Student Name</cite>
            <span class="testimonial__role">Speaking Client</span>
          </footer>
        </blockquote>
      </div>
    </div>
  </section>
  
  <!-- CTA Section -->
  <section class="cta bg-navy">
    <div class="container cta__container">
      <h2 class="cta__title">Ready to Discover What Your Voice Can Do?</h2>
      <p class="cta__text">
        Let's start with a conversation. Tell me about your goals, and we'll explore how we might work together.
      </p>
      <a href="/contact" class="btn btn--primary btn--large">Get Started</a>
    </div>
  </section>
</BaseLayout>

<style>
  /* ==========================================================================
     HERO SECTION
     ========================================================================== */
  
  .hero {
    padding-block: var(--space-16) var(--space-20);
    min-height: 70vh;
    display: flex;
    align-items: center;
  }
  
  .hero__container {
    display: grid;
    gap: var(--space-10);
    align-items: center;
  }
  
  @media (min-width: 1024px) {
    .hero__container {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-16);
    }
  }
  
  .hero__title {
    font-size: var(--heading-hero);
    color: var(--color-white);
    margin-bottom: var(--space-4);
  }
  
  .hero__subtitle {
    font-size: var(--text-xl);
    color: var(--color-cream);
    opacity: 0.9;
    margin-bottom: var(--space-8);
    max-width: 540px;
  }
  
  .hero__cta {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4);
  }
  
  .hero__image-placeholder {
    aspect-ratio: 4 / 5;
    max-width: 400px;
    margin-inline: auto;
    background: linear-gradient(135deg, var(--color-navy-medium), var(--color-navy-light));
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-cream);
    font-size: var(--text-sm);
    opacity: 0.6;
  }
  
  /* ==========================================================================
     BUTTONS
     ========================================================================== */
  
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-6);
    font-size: var(--text-base);
    font-weight: var(--font-semibold);
    text-decoration: none;
    border-radius: var(--radius-md);
    transition: all var(--transition-fast);
    cursor: pointer;
    border: 2px solid transparent;
  }
  
  .btn--primary {
    background-color: var(--color-gold-bright);
    color: var(--color-navy-deep);
  }
  
  .btn--primary:hover {
    background-color: var(--color-gold-hover);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }
  
  .btn--secondary {
    background-color: transparent;
    color: var(--color-white);
    border-color: var(--color-cream);
  }
  
  .btn--secondary:hover {
    background-color: rgba(255, 255, 255, 0.1);
    color: var(--color-gold-bright);
    border-color: var(--color-gold-bright);
  }
  
  .btn--avf {
    background-color: var(--color-avf-amber);
    color: var(--color-navy-deep);
  }
  
  .btn--avf:hover {
    background-color: var(--color-avf-amber-hover);
  }
  
  .btn--pass {
    background-color: var(--color-pass-green);
    color: var(--color-navy-deep);
  }
  
  .btn--pass:hover {
    background-color: var(--color-pass-green-hover);
  }
  
  .btn--large {
    padding: var(--space-4) var(--space-8);
    font-size: var(--text-lg);
  }
  
  /* ==========================================================================
     INTRO SECTION
     ========================================================================== */
  
  .intro {
    padding-block: var(--section-padding-y);
  }
  
  .intro__container {
    max-width: var(--container-narrow);
    text-align: center;
  }
  
  .intro__title {
    margin-bottom: var(--space-6);
  }
  
  .intro__text {
    font-size: var(--text-lg);
    color: var(--color-charcoal-light);
  }
  
  /* ==========================================================================
     SERVICES SECTION
     ========================================================================== */
  
  .services {
    padding-block: var(--section-padding-y);
  }
  
  .services__title {
    margin-bottom: var(--space-12);
  }
  
  .services__grid {
    display: grid;
    gap: var(--space-8);
  }
  
  @media (min-width: 768px) {
    .services__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  
  .service-card {
    background: var(--color-white);
    padding: var(--space-8);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-card);
    transition: transform var(--transition-base), box-shadow var(--transition-base);
  }
  
  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-card-hover);
  }
  
  .service-card__icon {
    color: var(--color-gold-bright);
    margin-bottom: var(--space-4);
  }
  
  .service-card__title {
    margin-bottom: var(--space-3);
  }
  
  .service-card__text {
    color: var(--color-charcoal-light);
    margin-bottom: var(--space-4);
  }
  
  .service-card__link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    color: var(--color-gold-muted);
    font-weight: var(--font-semibold);
    text-decoration: none;
    transition: color var(--transition-fast), gap var(--transition-fast);
  }
  
  .service-card__link:hover {
    color: var(--color-gold-bright);
    gap: var(--space-3);
  }
  
  /* ==========================================================================
     AUTHORITY SECTION
     ========================================================================== */
  
  .authority {
    padding-block: var(--section-padding-y);
  }
  
  .authority__title {
    color: var(--color-white);
    margin-bottom: var(--space-4);
  }
  
  .authority__subtitle {
    color: var(--color-cream);
    max-width: 600px;
    margin-inline: auto;
    margin-bottom: var(--space-12);
    opacity: 0.9;
  }
  
  .authority__grid {
    display: grid;
    gap: var(--space-8);
  }
  
  @media (min-width: 768px) {
    .authority__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  .authority-card {
    display: grid;
    gap: var(--space-6);
    padding: var(--space-8);
    border-radius: var(--radius-lg);
  }
  
  @media (min-width: 640px) {
    .authority-card {
      grid-template-columns: 1fr auto;
      align-items: center;
    }
  }
  
  .authority-card--avf {
    background-color: var(--color-avf-teal);
  }
  
  .authority-card--avf .authority-card__title {
    color: var(--color-avf-amber);
  }
  
  .authority-card--pass {
    background-color: var(--color-pass-teal);
  }
  
  .authority-card--pass .authority-card__title {
    color: var(--color-pass-green);
  }
  
  .authority-card__text {
    color: var(--color-cream);
    opacity: 0.9;
    margin-bottom: var(--space-4);
  }
  
  .authority-card__image-placeholder {
    width: 120px;
    height: 160px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-cream);
    font-size: var(--text-sm);
    opacity: 0.6;
  }
  
  /* ==========================================================================
     TESTIMONIALS SECTION
     ========================================================================== */
  
  .testimonials {
    padding-block: var(--section-padding-y);
  }
  
  .testimonials__title {
    margin-bottom: var(--space-12);
  }
  
  .testimonials__grid {
    display: grid;
    gap: var(--space-8);
  }
  
  @media (min-width: 768px) {
    .testimonials__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  .testimonial {
    background: var(--color-white);
    padding: var(--space-8);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-card);
    border-left: 4px solid var(--color-gold-bright);
  }
  
  .testimonial__text {
    font-size: var(--text-lg);
    font-style: italic;
    color: var(--color-charcoal-light);
    margin-bottom: var(--space-4);
  }
  
  .testimonial__footer {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }
  
  .testimonial__author {
    font-weight: var(--font-semibold);
    color: var(--color-charcoal);
    font-style: normal;
  }
  
  .testimonial__role {
    font-size: var(--text-sm);
    color: var(--color-gray-medium);
  }
  
  /* ==========================================================================
     CTA SECTION
     ========================================================================== */
  
  .cta {
    padding-block: var(--section-padding-y);
    text-align: center;
  }
  
  .cta__container {
    max-width: var(--container-narrow);
  }
  
  .cta__title {
    color: var(--color-white);
    margin-bottom: var(--space-4);
  }
  
  .cta__text {
    color: var(--color-cream);
    font-size: var(--text-lg);
    margin-bottom: var(--space-8);
    opacity: 0.9;
  }
</style>
