---
import Layout from '../layouts/BaseLayout.astro';
---

<Layout 
  title="Get Started | Ted's Voice Academy" 
  description="Ready to work on your voice? Contact Ted's Voice Academy for singing, speaking, and ensemble coaching in Lacey, WA or virtually."
>
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <span class="hero-eyebrow">Contact</span>
      <h1>Get Started</h1>
      <p class="hero-subtitle">Ready to work on your voice? Here's how to begin.</p>
    </div>
  </section>

  <!-- Intro + Form Section -->
  <section class="form-section">
    <div class="container">
      <div class="form-grid">
        <!-- Left: Intro + Form -->
        <div class="form-column">
          <p class="intro-text">
            The first step is simple — tell me a bit about yourself and what you're hoping to accomplish. I'll follow up within 24–48 hours to schedule a free discovery call. That's where we'll talk through your goals and see if we're a good fit.
          </p>

          <h2 class="form-heading">Tell me about yourself</h2>
          
          <form 
            name="contact" 
            method="POST" 
            data-netlify="true"
            netlify-honeypot="bot-field"
            class="contact-form"
          >
            <!-- Honeypot field for spam protection -->
            <p class="hidden">
              <label>Don't fill this out if you're human: <input name="bot-field" /></label>
            </p>
            <input type="hidden" name="form-name" value="contact" />
            
            <!-- Name -->
            <div class="form-group">
              <label for="name">Your name <span class="required">*</span></label>
              <input type="text" id="name" name="name" required />
            </div>

            <!-- Email -->
            <div class="form-group">
              <label for="email">Email address <span class="required">*</span></label>
              <input type="email" id="email" name="email" required />
            </div>

            <!-- Phone -->
            <div class="form-group">
              <label for="phone">Phone number (optional)</label>
              <input type="tel" id="phone" name="phone" />
            </div>

            <!-- Interest (Checkboxes) -->
            <fieldset class="form-group">
              <legend>I'm interested in... <span class="required">*</span></legend>
              <div class="checkbox-group">
                <label class="checkbox-label">
                  <input type="checkbox" name="interest[]" value="singing" />
                  <span>Singing</span>
                </label>
                <label class="checkbox-label">
                  <input type="checkbox" name="interest[]" value="speaking" />
                  <span>Speaking</span>
                </label>
                <label class="checkbox-label">
                  <input type="checkbox" name="interest[]" value="ensemble" />
                  <span>Ensemble Coaching</span>
                </label>
              </div>
            </fieldset>

            <!-- Experience Level -->
            <div class="form-group">
              <label for="experience">Experience level <span class="required">*</span></label>
              <select id="experience" name="experience" required>
                <option value="">Select your experience level</option>
                <option value="beginner">Beginner</option>
                <option value="some-experience">Some experience</option>
                <option value="intermediate">Intermediate</option>
                <option value="advanced">Advanced</option>
              </select>
            </div>

            <!-- Goals -->
            <div class="form-group">
              <label for="goals">What are you hoping to work on? <span class="required">*</span></label>
              <textarea id="goals" name="goals" rows="4" required></textarea>
            </div>

            <!-- Referral Source -->
            <div class="form-group">
              <label for="referral">How did you hear about me?</label>
              <select id="referral" name="referral">
                <option value="">Select an option</option>
                <option value="web-search">Web search</option>
                <option value="referral">Referral</option>
                <option value="social-media">Social media</option>
                <option value="other">Other</option>
              </select>
            </div>

            <!-- Lesson Format -->
            <fieldset class="form-group">
              <legend>Preferred lesson format</legend>
              <div class="radio-group">
                <label class="radio-label">
                  <input type="radio" name="format" value="in-person" />
                  <span>In-person (Lacey, WA)</span>
                </label>
                <label class="radio-label">
                  <input type="radio" name="format" value="virtual" />
                  <span>Virtual</span>
                </label>
                <label class="radio-label">
                  <input type="radio" name="format" value="either" />
                  <span>Either</span>
                </label>
              </div>
            </fieldset>

            <!-- Additional Info -->
            <div class="form-group">
              <label for="additional">Anything else I should know?</label>
              <textarea id="additional" name="additional" rows="3"></textarea>
            </div>

            <!-- Submit -->
            <button type="submit" class="btn btn-primary">Send Message</button>
          </form>
        </div>

        <!-- Right: What Happens Next + Contact Info -->
        <div class="info-column">
          <!-- What Happens Next -->
          <div class="info-card">
            <h3>What to expect</h3>
            <p>After you submit this form, here's what happens:</p>
            <ol class="expect-list">
              <li>I'll review your information and respond within 24–48 hours.</li>
              <li>We'll schedule a free discovery call — a short conversation where I learn more about your goals and you learn how I work.</li>
              <li>If we're a good fit, we'll book your first lesson.</li>
            </ol>
            <p class="no-pressure">No pressure. No obligations. Just a conversation.</p>
          </div>

          <!-- Alternative Contact -->
          <div class="info-card">
            <h3>Other ways to reach me</h3>
            
            <div class="contact-method">
              <h4>Email</h4>
              <a href="mailto:ted@tedsvoiceacademy.com">ted@tedsvoiceacademy.com</a>
            </div>

            <div class="contact-method">
              <h4>Phone</h4>
              <a href="tel:+12534142267">253-414-2267</a>
            </div>

            <div class="contact-method">
              <h4>Studio Location</h4>
              <address>
                4204 Lorna Ct SE<br />
                Lacey, WA 98503
              </address>
              <a 
                href="https://www.google.com/maps/search/?api=1&query=4204+Lorna+Ct+SE+Lacey+WA+98503" 
                target="_blank" 
                rel="noopener noreferrer"
                class="map-link"
              >
                View on Google Maps →
              </a>
              <p class="parking-note">Plenty of street parking available.</p>
            </div>
          </div>

          <!-- Specialized Inquiries -->
          <div class="info-card info-card-accent">
            <h3>Looking for something specific?</h3>
            
            <div class="contact-method">
              <h4>PASS Profile questions</h4>
              <a href="mailto:PASS@tedsvoiceacademy.com">PASS@tedsvoiceacademy.com</a>
            </div>

            <div class="contact-method">
              <h4>AVF book inquiries</h4>
              <a href="mailto:AVF@tedsvoiceacademy.com">AVF@tedsvoiceacademy.com</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Map Section -->
  <section class="map-section">
    <div class="container">
      <h2 class="section-heading">Find the Studio</h2>
      <div class="map-container">
        <iframe
          src="https://www.google.com/maps?q=4204+Lorna+Ct+SE,+Lacey,+WA+98503&output=embed"
          width="100%"
          height="400"
          style="border:0;"
          allowfullscreen=""
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          title="Ted's Voice Academy Studio Location — 4204 Lorna Ct SE, Lacey, WA 98503"
        ></iframe>
      </div>
      <p class="directions-link">
        <a
          href="https://www.google.com/maps/dir/?api=1&destination=4204+Lorna+Ct+SE,+Lacey,+WA+98503"
          target="_blank"
          rel="noopener noreferrer"
        >
          Get Directions &rarr;
        </a>
      </p>
    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .hero {
    background: linear-gradient(135deg, var(--color-navy-deep) 0%, var(--color-navy-medium) 100%);
    padding: 4rem 0 3rem;
    text-align: center;
  }

  .hero-eyebrow {
    display: block;
    font-size: 1rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-gold-bright);
    margin-bottom: 0.75rem;
  }

  .hero h1 {
    font-family: var(--font-heading);
    font-size: clamp(2.5rem, 5vw + 1rem, 3.75rem);
    font-weight: 700;
    color: var(--color-white);
    margin: 0 0 1rem;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--color-cream);
    max-width: 600px;
    margin: 0 auto;
  }

  /* Form Section */
  .form-section {
    background: var(--color-off-white);
    padding: 4rem 0;
  }

  .form-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  @media (min-width: 1024px) {
    .form-grid {
      grid-template-columns: 1.2fr 1fr;
      gap: 4rem;
    }
  }

  .intro-text {
    font-size: 1.125rem;
    line-height: 1.7;
    color: var(--color-charcoal);
    margin-bottom: 2rem;
  }

  .form-heading {
    font-family: var(--font-heading);
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--color-navy-deep);
    margin: 0 0 1.5rem;
  }

  /* Form Styles */
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .form-group label,
  .form-group legend {
    font-weight: 500;
    color: var(--color-charcoal);
    font-size: 0.9375rem;
  }

  fieldset.form-group {
    border: none;
    padding: 0;
    margin: 0;
  }

  .required {
    color: var(--color-gold-bright);
  }

  input[type="text"],
  input[type="email"],
  input[type="tel"],
  select,
  textarea {
    padding: 0.875rem 1rem;
    font-size: 1rem;
    border: 2px solid #e0ddd5;
    border-radius: 6px;
    background: var(--color-white);
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
    font-family: var(--font-body);
  }

  input:focus,
  select:focus,
  textarea:focus {
    outline: none;
    border-color: var(--color-gold-bright);
    box-shadow: 0 0 0 3px rgba(212, 168, 75, 0.15);
  }

  textarea {
    resize: vertical;
    min-height: 100px;
  }

  /* Checkbox & Radio Styles */
  .checkbox-group,
  .radio-group {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: 0.5rem;
  }

  .checkbox-label,
  .radio-label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    cursor: pointer;
    font-weight: 400;
  }

  .checkbox-label input,
  .radio-label input {
    width: 1.125rem;
    height: 1.125rem;
    accent-color: var(--color-gold-bright);
  }

  /* Submit Button */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 1rem 2rem;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 6px;
    cursor: pointer;
    transition: all 0.2s ease;
    border: none;
    font-family: var(--font-body);
  }

  .btn-primary {
    background: var(--color-gold-bright);
    color: var(--color-navy-deep);
  }

  .btn-primary:hover {
    background: var(--color-gold-hover);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(212, 168, 75, 0.3);
  }

  /* Hidden honeypot */
  .hidden {
    display: none;
  }

  /* Info Column */
  .info-column {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .info-card {
    background: var(--color-white);
    padding: 1.75rem;
    border-radius: 12px;
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
  }

  .info-card h3 {
    font-family: var(--font-heading);
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-navy-deep);
    margin: 0 0 1rem;
  }

  .info-card p {
    color: var(--color-charcoal);
    line-height: 1.6;
    margin: 0 0 1rem;
  }

  .expect-list {
    margin: 1rem 0;
    padding-left: 1.5rem;
    color: var(--color-charcoal);
  }

  .expect-list li {
    margin-bottom: 0.75rem;
    line-height: 1.6;
  }

  .no-pressure {
    font-style: italic;
    color: var(--color-gold-bright);
    font-weight: 500;
    margin: 0;
  }

  .contact-method {
    margin-bottom: 1.25rem;
  }

  .contact-method:last-child {
    margin-bottom: 0;
  }

  .contact-method h4 {
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-navy-deep);
    margin: 0 0 0.25rem;
  }

  .contact-method a {
    color: var(--color-gold-bright);
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s ease;
  }

  .contact-method a:hover {
    color: var(--color-gold-hover);
  }

  .contact-method address {
    font-style: normal;
    color: var(--color-charcoal);
    line-height: 1.5;
  }

  .map-link {
    display: inline-block;
    margin-top: 0.5rem;
  }

  .parking-note {
    font-size: 0.875rem;
    color: #666;
    margin-top: 0.5rem;
    margin-bottom: 0;
  }

  /* Accent Card */
  .info-card-accent {
    background: linear-gradient(135deg, var(--color-navy-deep) 0%, var(--color-navy-medium) 100%);
  }

  .info-card-accent h3 {
    color: var(--color-gold-bright);
  }

  .info-card-accent .contact-method h4 {
    color: var(--color-cream);
  }

  .info-card-accent .contact-method a {
    color: var(--color-gold-bright);
  }

  /* Map Section */
  .map-section {
    background: var(--color-navy-deep);
    padding: 3rem 0 4rem;
  }

  .section-heading {
    font-family: var(--font-heading);
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--color-white);
    text-align: center;
    margin: 0 0 1.5rem;
  }

  .map-container {
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
  }

  .map-container iframe {
    display: block;
    width: 100%;
    height: 400px;
  }

  .directions-link {
    text-align: center;
    margin-top: 1rem;
  }

  .directions-link a {
    color: var(--color-gold-bright);
    text-decoration: none;
    font-weight: 600;
    font-size: 1rem;
    transition: color 0.2s ease;
  }

  .directions-link a:hover {
    color: var(--color-gold-hover);
    text-decoration: underline;
  }

  /* Mobile Adjustments */
  @media (max-width: 768px) {
    .hero {
      padding: 3rem 0 2rem;
    }

    .form-section {
      padding: 2.5rem 0;
    }

    .checkbox-group,
    .radio-group {
      flex-direction: column;
      gap: 0.75rem;
    }

    .map-container iframe {
      height: 300px;
    }
  }
</style>
